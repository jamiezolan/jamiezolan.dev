StylesPath = styles
MinAlertLevel = suggestion

# Run `vale sync` to download these packages before first use.
Packages = Microsoft, Google

[*.md]
BasedOnStyles = Vale, Microsoft, Google

# Don't lint code, the generated reference, or the spec.
Microsoft.Quotes = NO

[reference/*]
BasedOnStyles =
