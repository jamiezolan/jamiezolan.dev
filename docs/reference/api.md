# API reference

The reference below is generated directly from the [OpenAPI 3.1 specification](openapi.yaml) that lives in this repository. Because the spec is the single source of truth, the docs and the API can't drift apart — regenerate and the reference updates itself.

!!! tip "Source of truth"
    The same `openapi.yaml` can generate this human-readable reference, client SDKs, server stubs, and contract tests. Editing the spec is how you change all of them at once.

<iframe
  src="../api-embed.html"
  title="Pudgy API reference"
  style="width:100%; height:820px; border:1px solid var(--md-default-fg-color--lightest); border-radius:.5rem;"
  loading="lazy">
</iframe>

---

Prefer the raw machine-readable contract? [Download `openapi.yaml`](openapi.yaml).
